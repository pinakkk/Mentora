// Prisma client singleton
// Note: We primarily use Supabase client for queries.
// Prisma is used for migrations and schema management.
// To use Prisma client, configure the adapter in prisma.config.ts

export {}; // placeholder — all DB access goes through Supabase client
